<!DOCTYPE html>  
<html lang="en">  
    <head>  
        <title>Beeva - Formación Externa - SPA Auth Control</title>
        <meta charset="utf-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
    </head>  
    <body>  
    <script type="text/javascript">  
        if (window.opener) {
            window.opener.focus();

            var state = '<%= state %>';
            var user = <%- JSON.stringify(userData) %>;
            var token = '<%= token %>';
            var message = '<%= message %>'

            window.opener.PFE.authService(state, user, token, message);

        }
        window.close();
    </script>  
    </body>  
</html>